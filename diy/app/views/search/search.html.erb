<div id='tag_cloud'>
    <% tag_cloud  Project.tag_counts, %w[s s s] do |tag, css_class| %>
    <%= link_to tag.name, tag_path(tag.name), class:css_class  %>
<% end %>
</div>
<section class="projects-index">
<div class="projects">
    <%= render @projects %>
</div>
</section>